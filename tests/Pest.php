<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Pest — architecture tests only
|--------------------------------------------------------------------------
|
| This app's test suite runs on PHPUnit (`php artisan test` /
| `vendor/bin/phpunit`) — Pest is installed solely for `arch()` rules,
| which PHPUnit has no equivalent for. Run them with:
|
|   vendor/bin/pest tests/Architecture
|
| Every other test in tests/Feature and tests/Unit stays on PHPUnit and is
| untouched by this file.
*/
